# --- Backend web ---
fastapi==0.110.0          # Framework leve e rápido para APIs
uvicorn[standard]==0.29.0 # Servidor ASGI para rodar o FastAPI

# --- Templates e arquivos estáticos ---
jinja2==3.1.3             # Engine para templates HTML (interface)
aiofiles==23.2.1          # Manipulação assíncrona de arquivos (upload)

# --- Azure AI ---
azure-ai-formrecognizer==3.3.2  # Cliente oficial do Azure Form Recognizer
azure-core==1.30.1              # Dependência base da Azure SDK

# --- Manipulação de PDFs (caso use localmente) ---
PyMuPDF==1.24.2          # Biblioteca para extrair texto de PDFs (opcional)
pdfplumber==0.10.3       # Extração avançada de conteúdo de PDFs (backup/local)

# --- Regex, validação, utils ---
python-multipart==0.0.9  # Necessário para upload de arquivos via formulário
python-dotenv==1.0.1     # Para ler variáveis de ambiente (como chave Azure)

# --- Logging / debug (opcional, mas útil) ---
loguru==0.7.2            # Logger moderno e poderoso

# --- Parser e manipulação de texto (caso use NLP depois) ---
regex==2023.12.25
